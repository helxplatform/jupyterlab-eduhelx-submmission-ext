/**
 * Example of [Jest](https://jestjs.io/docs/getting-started) unit tests
 */

describe('eduhelx_jupyterlab_prof', () => {
  it('should have some tests written at some point maybe?', () => {
    expect(1 + 1).toEqual(2);
  });
});
