import { style } from 'typestyle'

export const collapseContainerClass = style({
    
})

export const collapsePanelContainerClass = style({

})

export const collapsePanelHeaderClass = style({
    display: 'flex',
    padding: '12px 16px'
})